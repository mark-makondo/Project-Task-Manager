import React from 'react';

// ui
import OwnedProjects from './OwnedProjects';

const OwnedProjetsContainer = ({ projects }) => {
	return <OwnedProjects projects={projects} />;
};

export default OwnedProjetsContainer;
